name: Cache Test

on:
  push:
  workflow_dispatch:

jobs:
  cache-job:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Cache dependencies
        id: cache-step
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache
          key: cache-23c313f

      - name: prime-cache-23c313f
        run: echo "Cache hit: ${{ steps.cache-step.outputs.cache-hit }}"
